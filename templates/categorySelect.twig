{% include "./common/header.twig" %}


<section>
	
	<div class="container">

		<div class="text-center">
				<h2>List of Category</h2>
		</div>
		<hr style="border-color: #e74c3c;">
		<br>
		<div class="table-responsive">
            <div class="well">
			<table class="table">
				<thead>
			      <tr>
			      	<th>ID</th>
			        <th>Category</th>
			        <th>Action</th>
			      </tr>
	    		</thead>
               
	    		<tbody>
			        {% for category in categoryDetails  %}
			        	<tr>
			        		<td>{{category.id}}</td>
					        <td>{{category.category}}</td>
					        <td>
					        <a class="buttonNew" href="/category/view/{{category.category}}">View Category</a>
					        </td>
				      	</tr>
			        {% endfor %}
		      	</tbody>

			</table>
        
		</div>
        </div>
	</div>
</section>

{% include "./common/footer.twig" %}