{% include "./common/header.twig" %}


<section>
	
	<div class="container">

		<div class="text-center">
			<h2>List of Category</h2>
		</div>
		<hr style="border-color: #e74c3c;">
		<br>
        <div class="well"
		<div class="table-responsive">

			<table class="table table">
				<thead>
			      <tr>
			      	<th>ID</th>
			        <th>Category</th>
			        <th>Action</th>
			      </tr>
	    		</thead>

	    		<tbody>
			        {% for category in categoryDetails  %}
			        	<tr>
			        		<td>{{ category.id }}</td>
					        <td>{{ category.category }}</td>
					        <td>
					        	<a class="buttonNew" href="/category/view/{{category.category}}">View</a>
                                
					        	<a class="buttonNew" href="/categoryUpdate/{{ category.id }}">Update</a>
					        	
					        	<a class="buttonNew"href="/categoryDelete/{{ category.id }}">Delete</a>
					        </td>
				      	</tr>
			        {% endfor %}
		      	</tbody>

			</table>
		</div>
    </div>
	</div>
</section>

{% include "./common/footer.twig" %}